package com.tradeix.contractcomposition.contracts.common

import net.corda.core.transactions.LedgerTransaction

/*
class StandardTransactionFieldResolution: FieldRetriever {
    override fun retrieveField(objToCast: Any, fieldIdentifier: Any): Any {
        // First, cast the given object as a standard Corda LedgerTransaction
        val tx = objToCast as LedgerTransaction


    }
} */